#include <bits/stdc++.h>

using namespace std;

int main() {
    int n_tests; cin >> n_tests;

    for (int i=0; i<n_tests; ++i){
        string num_to_count; cin >> num_to_count;
        cout << num_to_count.size() << "\n";
    }
    
}